from django.apps import AppConfig

class BoletosConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'revisao_segura.boletos'  # ⚠️ Certifique-se de que está apenas 'boletos'
